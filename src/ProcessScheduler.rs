use std::*; //use of external libraries are fine for now but should be removed later
pub mod main;
struct Process {
    field: String, //lib dependency
    processor: u8
}

impl Process for Process {
}

struct Schedular {
    //
}
